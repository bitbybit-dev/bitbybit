// Generated by scripts/gen-ts-references.mjs. The typecheck-only strict overlay: this package's build
// config, then the shared strict flags in tsconfig.base.cad-strict.json; `npm run typecheck:strict` runs
// it through tsc-baseline against .tsc-baseline.json. The build config's references are repeated here
// because references are not inherited through extends; without them the overlay would follow a
// sibling's declarations into the source of a package this one never resolves through its own paths.
{
    "extends": [
        "./tsconfig.bitbybit.json",
        "../../../tsconfig.base.cad-strict.json"
    ],
    "references": [
        {
            "path": "../base/tsconfig.bitbybit.json"
        },
        {
            "path": "../occt/tsconfig.bitbybit.json"
        }
    ]
}
